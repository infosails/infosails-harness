# Agentes internos — Bug

Solo el **Bug Lead** los conoce y activa.

| id | Rol | Salida |
|----|-----|--------|
| `triager` | Acotar el defecto con el usuario | Borrador del Bug Brief |
| `scribe` | Persistir en memoria | `memory/bugs/*` |

Extensión futura (ej. `repro-runner`) sin cambiar el contrato outbox con la Orquesta.
